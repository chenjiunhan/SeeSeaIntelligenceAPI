version: '3.9'

# Production overrides for SSL and domain configuration
services:
  nginx:
    volumes:
      # Override to use SSL config
      - ../nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /var/www/certbot:/var/www/certbot:ro
